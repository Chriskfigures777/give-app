{
  "name": "stripe-webhook-handler",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node22 --format=esm --outfile=dist/index.mjs",
    "package": "cd dist && zip -r ../dist.zip index.mjs",
    "deploy": "pnpm run build && pnpm run package"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.95.3",
    "resend": "^6.9.2",
    "stripe": "^20.3.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.2"
  }
}
