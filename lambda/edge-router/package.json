{
  "name": "give-edge-router",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node18 --outfile=dist/index.mjs --format=esm --external:@aws-sdk/*"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "@aws-sdk/client-s3": "^3.700.0"
  }
}
